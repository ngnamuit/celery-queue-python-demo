{
    "broker_url"       : "amqp://guest:guest@localhost:5672//",
    "backend_url"      : "mongodb://localhost:27018/app_demo",
    "task_always_eager": true, // default is False  and set True for testing environment - `True` will execute task immediately, no delay
    "flower_address"   : "localhost",
    "flower_port"      : 5555,
    "from_email"       : "your_email_login_sendgrid",
    "sendgrid_api_key" : "your_sendgrid_api_key"
}